import React from 'react';

// Keep the type definitions for use in other components
export type TabType = 'rfp' | 'floorplan' | 'vendors';
export type FloorplanSubTab = 'tables' | 'seating' | 'staging' | 'av';

// Remove the component implementation since it's redundant with the header tabs 